{
  "name": "lambda_gateway",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "build": "rm -rf dist && node ./scripts/build.mjs"
  },
  "dependencies": {
    "@slack/bolt": "^3.12.2",
    "openai": "^3.2.1"
  },
  "devDependencies": {
    "@aws-sdk/client-lambda": "^3.299.0",
    "@aws-sdk/client-ssm": "^3.295.0",
    "@types/aws-lambda": "^8.10.114",
    "esbuild": "^0.17.12"
  }
}